#pragma once

#include "Render.h"
#include <vector>
#include <string>

namespace ModelFactory {

    RenderableModel createCube();
    RenderableModel createLine();
    RenderableModel createPyramid();
    
    // Сюда можно будет добавлять другие функции для создания моделей

}